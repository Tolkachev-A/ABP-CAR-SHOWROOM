import './header.scss';

export const Header = () => {
  return (
    <header className={'container'}>
      <div className={'header heading-font'}>
        <span className={'ascent'}>CAR</span> SHOWROOM
      </div>
    </header>
  );
};
